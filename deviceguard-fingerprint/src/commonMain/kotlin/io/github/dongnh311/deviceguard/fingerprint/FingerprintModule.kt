package io.github.dongnh311.deviceguard.fingerprint

/** Identifier constants for the fingerprint module. Internal — consumers read from instances. */
internal object FingerprintModule {
    internal const val DETECTOR_ID: String = "fingerprint"
    internal const val SCHEMA_VERSION: Int = 1
}
